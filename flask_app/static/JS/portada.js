let formulario = document.getElementById("crear_aviso");
formulario.addEventListener("click", () => {
            window.location.href = "../HTML/crear_aviso_adopcion.html"
        });

let listado = document.getElementById("listado");
listado.addEventListener("click", () => {
            window.location.href = "../HTML/listado.html"
        });

let stats = document.getElementById("estadisticas");
stats.addEventListener("click", () => {
            window.location.href = "../HTML/estadisticas.html"
        });